import React from 'react';
import { AbsoluteFill, useCurrentFrame } from 'remotion';
import { SceneBase } from './SceneBase';
import { channelThemes, normalizeChannel } from '../styles/channelThemes';
export const CodeTerminalScene: React.FC<any> = ({ channel, scene, progress }) => { const theme = channelThemes[normalizeChannel(channel)]; const frame=useCurrentFrame(); const lines=['nmap -sV target.local','22/tcp open ssh','443/tcp open https','auth.log: failed password']; return <SceneBase channel={channel} scene={scene} progress={progress}><AbsoluteFill style={{ justifyContent:'center', alignItems:'center' }}><div style={{ width:1280, background:'#08131d', border:`1px solid ${theme.palette.border}`, boxShadow:'0 30px 80px rgba(0,0,0,0.35)' }}><div style={{ padding:'14px 20px', borderBottom:`1px solid ${theme.palette.border}`, color: theme.palette.primary, fontFamily: theme.typography.mono }}>root@terminalecho:~</div><div style={{ padding:'28px 34px', fontFamily: theme.typography.mono, fontSize:28, lineHeight:1.5 }}>{lines.map((line,i)=><div key={i} style={{ color:i===0?theme.palette.primary:theme.palette.text, opacity: frame/10 > i ? 1 : 0.18 }}>{i===0?`$ ${line}`:line}</div>)}</div></div></AbsoluteFill></SceneBase>; };
