import React from 'react';
import { AbsoluteFill } from 'remotion';
import { SceneBase } from './SceneBase';
import { channelThemes, normalizeChannel } from '../styles/channelThemes';
export const ChecklistScene: React.FC<any> = ({ channel, scene, progress }) => { const theme = channelThemes[normalizeChannel(channel)]; const items = scene.keywords?.length ? scene.keywords : scene.narration_text.split(/[,.;]/).map((x:string)=>x.trim()).filter(Boolean).slice(0,4); return <SceneBase channel={channel} scene={scene} progress={progress}><AbsoluteFill style={{ justifyContent:'center', padding:'120px 150px' }}><div style={{ fontSize:78, fontWeight:900, marginBottom:30 }}>{scene.title}</div><div style={{ display:'grid', gridTemplateColumns:'1fr 1fr', gap:20 }}>{items.map((item:string, i:number)=><div key={i} style={{ display:'flex', gap:16, alignItems:'flex-start', padding:'20px 22px', background:'rgba(255,255,255,0.04)', border:`1px solid ${theme.palette.border}` }}><div style={{ width:34, height:34, borderRadius:10, background: i===0 ? theme.palette.primary : theme.palette.secondary, color:'#000', display:'flex', alignItems:'center', justifyContent:'center', fontWeight:900 }}>{i+1}</div><div style={{ fontSize:30, lineHeight:1.18 }}>{item}</div></div>)}</div></AbsoluteFill></SceneBase>; };
