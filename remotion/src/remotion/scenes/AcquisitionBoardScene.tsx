import React from 'react';
import { AbsoluteFill } from 'remotion';
import { SceneBase } from './SceneBase';
import { channelThemes, normalizeChannel } from '../styles/channelThemes';
export const AcquisitionBoardScene: React.FC<any> = ({ channel, scene, progress }) => { const theme = channelThemes[normalizeChannel(channel)]; const cols=['Sourcing','Review','LOI','Close']; return <SceneBase channel={channel} scene={scene} progress={progress}><AbsoluteFill style={{ justifyContent:'center', padding:'110px 90px' }}><div style={{ display:'grid', gridTemplateColumns:'repeat(4, 1fr)', gap:18 }}>{cols.map((col,i)=><div key={i} style={{ padding:18, minHeight:400, background:'rgba(255,255,255,0.03)', border:`1px solid ${theme.palette.border}` }}><div style={{ color: theme.palette.primary, fontSize:22, textTransform:'uppercase', letterSpacing:'0.12em', marginBottom:14 }}>{col}</div>{Array.from({length:2}).map((_,j)=><div key={j} style={{ padding:'16px', marginBottom:12, background:'rgba(255,255,255,0.04)', border:`1px solid ${j===0?theme.palette.secondary:theme.palette.border}` }}><div style={{ fontSize:24, fontWeight:700 }}>{scene.keywords?.[j] || 'Deal card'}</div><div style={{ color: theme.palette.muted, marginTop:8 }}>Cash flow, diligence, financing</div></div>)}</div>)}</div></AbsoluteFill></SceneBase>; };
