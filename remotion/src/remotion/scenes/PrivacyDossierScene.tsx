import React from 'react';
import { AbsoluteFill } from 'remotion';
import { SceneBase } from './SceneBase';
import { channelThemes, normalizeChannel } from '../styles/channelThemes';
export const PrivacyDossierScene: React.FC<any> = ({ channel, scene, progress }) => { const theme = channelThemes[normalizeChannel(channel)]; return <SceneBase channel={channel} scene={scene} progress={progress}><AbsoluteFill style={{ justifyContent:'center', padding:'100px 120px' }}><div style={{ display:'grid', gridTemplateColumns:'1.2fr 0.8fr', gap:26 }}><div style={{ padding:'26px', background:'rgba(255,255,255,0.03)', border:`1px solid ${theme.palette.border}` }}><div style={{ color: theme.palette.secondary, fontSize:20, letterSpacing:'0.18em', textTransform:'uppercase', marginBottom:16 }}>redacted dossier</div><div style={{ fontSize:64, fontWeight:900, marginBottom:24 }}>{scene.title}</div>{Array.from({length:5}).map((_,i)=><div key={i} style={{ height:18, marginBottom:14, width:`${85 - i*9}%`, background:i===2?'rgba(255,109,124,0.4)':'rgba(255,255,255,0.08)' }} />)}</div><div style={{ display:'grid', gap:18 }}>{['Device graph','Metadata trail','Signal leak'].map((card,i)=><div key={i} style={{ padding:'22px', border:`1px solid ${theme.palette.border}`, background:'rgba(255,255,255,0.035)' }}><div style={{ color: theme.palette.primary, fontSize:22, marginBottom:8 }}>{card}</div><div style={{ color: theme.palette.muted, fontSize:24 }}>{scene.keywords?.[i] || 'tracked surface'}</div></div>)}</div></div></AbsoluteFill></SceneBase>; };
