import React from 'react';
import { AbsoluteFill } from 'remotion';
import { SceneBase } from './SceneBase';
import { channelThemes, normalizeChannel } from '../styles/channelThemes';
export const DiagramScene: React.FC<any> = ({ channel, scene, progress }) => { const theme = channelThemes[normalizeChannel(channel)]; const nodes=[{x:280,y:320,label:scene.keywords?.[0]||'Input'},{x:960,y:220,label:scene.keywords?.[1]||'System'},{x:1540,y:520,label:scene.keywords?.[2]||'Outcome'}]; return <SceneBase channel={channel} scene={scene} progress={progress}><AbsoluteFill style={{ justifyContent: 'center', padding: 90 }}><svg width={1920} height={1080} viewBox="0 0 1920 1080" style={{ position:'absolute', inset:0 }}><line x1={nodes[0].x} y1={nodes[0].y} x2={nodes[1].x} y2={nodes[1].y} stroke={theme.palette.primary} strokeWidth={4} /><line x1={nodes[1].x} y1={nodes[1].y} x2={nodes[2].x} y2={nodes[2].y} stroke={theme.palette.secondary} strokeWidth={4} />{nodes.map((n,i)=><g key={i}><circle cx={n.x} cy={n.y} r={88} fill={theme.palette.surfaceAlt} stroke={i===1?theme.palette.secondary:theme.palette.primary} strokeWidth={3}/></g>)}</svg><div style={{ display:'flex', justifyContent:'space-between', marginTop:180 }}>{nodes.map((n,i)=><div key={i} style={{ width:320, marginLeft:i===0?110:0, marginRight:i===2?110:0, textAlign:'center' }}><div style={{ fontSize:36, fontWeight:800 }}>{n.label}</div><div style={{ color: theme.palette.muted, marginTop:14, fontSize:22 }}>{scene.keywords?.[i+1] || scene.scene_type}</div></div>)}</div></AbsoluteFill></SceneBase>; };
